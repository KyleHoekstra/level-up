// setting the var for hello
let hello = 'hello world';
// logging the value of hello
console.log(hello);
// changing the value of var hello
hello = 'goodbye';
// logging the value of hello
console.log(hello);
// creating an array for var numbers
let numbers = ['1', '2', '3', '4', '5'];
// logging the var numbers
console.log('numbers', numbers);
// adding the #6 to numbers array
numbers.push ('6');
// logging the var numbers
console.log( numbers );
// creating the var value
let value = 4;
// logging the var value
console.log( value == 3 );
// creating the var for boolean

let open = true;
console.log(!open);

// if statements checks something does something based on values
let myName = 'Kyl';
// checks to see if name is Kyle
if (myName === 'Kyle') {
  // OutPuts something
  console.log(myName,'is Awesome', 'ok');
} else {
  console.log('killroy');
}
// if myName is
let name = 'tood';
if (true) {
  console.log('ok im awesome' + name);
}

if (name !== "toddd") {
  console.log('todd');
}
if (2<=2) {
  console.log('yep');
} else {
  console.log('nope');
}
 let micNumber = 666;
 let killName = 'tom';
  if ( micNumber > 666 ) {
    console.log( name + " is cool" );
  } else if ( micNumber < 666) {
    console.log( myName + ' is way cooler' );
  } else {
    console.log( 'jumanju' );
  }
